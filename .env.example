# Public issuer URL. Production is https://id.entangleit.com
ISSUER=http://localhost:3000
PORT=3000

# 32+ character secrets. Generate with: openssl rand -hex 32
COOKIE_KEYS=change-me-to-a-long-random-string
SESSION_SECRET=change-me-to-another-long-random-string

# SQLite path. Use :memory: only in tests.
DATABASE_PATH=./data/twetch-oidc.sqlite

# Live production Twetch. Set false only for offline demo users.
TWETCH_LIVE=true
TWETCH_DEMO_MODE=false

# Production Twetch endpoints (twetch.com). The legacy *.twetch.app auth stack is down.
TWETCH_AUTH_URL=https://api.twetch.com
TWETCH_API_URL=https://api.twetch.com
TWETCH_GRAPHQL_URL=https://api.twetch.com
TWETCH_AUTH_FRONTEND_URL=https://twetch.com
